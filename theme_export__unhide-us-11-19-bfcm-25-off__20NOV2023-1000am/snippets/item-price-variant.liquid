{% if variant.compare_at_price > variant.price %}
	<s>{{ variant.compare_at_price | money | remove: '.00' }}</s>
	<span>{{ variant.price | money | remove: '.00' }}</span>
{% else %}
	<span>{{ variant.price | money | remove: '.00' }}</span>
{% endif %}
